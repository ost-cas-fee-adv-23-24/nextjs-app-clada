/** @type {import('next').NextConfig} */
const nextConfig = {
  logging: {
    fetches: {
      fullUrl: true,
    },
  },
  images: {
    domains: ['storage.googleapis.com'],
  },
};

module.exports = nextConfig;
